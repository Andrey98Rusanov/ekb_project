# Инструкция по настройке и запуску проекта

## 📋 Предварительные требования

- Node.js версии 18 или выше
- npm или yarn

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
cd ai-dashboard-app
npm install
```

### 2. Настройка окружения (опционально)

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Отредактируйте `.env` при необходимости:
- `API_URL` - URL вашего AI backend API
- `PORT` - порт для dev сервера (по умолчанию 3000)

### 3. Запуск в режиме разработки

```bash
npm run dev
```

Приложение будет доступно по адресу: http://localhost:3000

### 4. Сборка для production

```bash
npm run build
```

Собранные файлы будут в папке `build/`

## 🎯 Основные возможности

### Авторизация
- Страница входа: `/login`
- Страница регистрации: `/register`
- Валидация форм с помощью Formik + Yup
- Управление состоянием через Redux Toolkit

### Главная страница
- Интерактивные дашборды с графиками (Recharts)
- 4 карточки статистики
- График аналитики
- Блок последней активности

### AI Чат
- Интерфейс чата в правой панели
- Готов к интеграции с AI backend
- История сообщений хранится в Redux

## 🔌 Интеграция с AI Backend

### Настройка API

Откройте файл `src/utils/api.ts` и обновите методы для работы с вашим AI backend:

```typescript
export const chatApi = {
  sendMessage: async (message: string) => {
    const response = await apiClient.post('/chat/message', { message });
    return response.data;
  },
};
```

### Конфигурация endpoint'ов

В файле `src/utils/constants.ts` настройте endpoint'ы вашего API:

```typescript
export const API_ENDPOINTS = {
  CHAT: {
    SEND_MESSAGE: '/chat/message',
    // ... другие endpoint'ы
  },
};
```

### Пример интеграции

```typescript
// В компоненте ChatSection.tsx

const handleSendMessage = async (e: React.FormEvent) => {
  e.preventDefault();
  if (!inputValue.trim() || isLoading) return;

  const userMessage = {
    id: Date.now().toString(),
    text: inputValue,
    sender: 'user' as const,
    timestamp: new Date(),
  };

  dispatch(addMessage(userMessage));
  setInputValue('');
  dispatch(setLoading(true));

  try {
    // Отправка сообщения на AI backend
    const response = await chatApi.sendMessage(inputValue);
    
    const aiMessage = {
      id: (Date.now() + 1).toString(),
      text: response.message,
      sender: 'ai' as const,
      timestamp: new Date(),
    };
    
    dispatch(addMessage(aiMessage));
  } catch (error) {
    console.error('Error sending message:', error);
    // Обработка ошибки
  } finally {
    dispatch(setLoading(false));
  }
};
```

## 📁 Структура проекта

```
ai-dashboard-app/
├── public/
│   └── index.html              # HTML шаблон
├── src/
│   ├── app/
│   │   ├── App.tsx             # Главный компонент приложения
│   │   └── routes/             # Конфигурация роутинга
│   ├── components/
│   │   ├── Header.tsx          # Шапка приложения
│   │   ├── LoadingSpinner.tsx  # Компонент загрузки
│   │   ├── chat/               # Компоненты чата
│   │   └── dashboard/          # Компоненты дашборда
│   ├── pages/
│   │   ├── auth/               # Страницы авторизации
│   │   └── main/               # Главная страница
│   ├── store/
│   │   ├── index.ts            # Конфигурация Redux store
│   │   ├── hooks.ts            # Типизированные хуки Redux
│   │   └── slices/             # Redux slices
│   ├── styles/
│   │   └── index.css           # Глобальные стили и Tailwind
│   ├── types/
│   │   └── index.ts            # TypeScript типы
│   ├── utils/
│   │   ├── api.ts              # API клиент и методы
│   │   ├── constants.ts        # Константы приложения
│   │   └── helpers.ts          # Вспомогательные функции
│   └── index.tsx               # Точка входа
├── package.json
├── tsconfig.json
├── webpack.config.ts
├── tailwind.config.js
└── README.md
```

## 🎨 Кастомизация

### Цвета

Измените цветовую схему в `tailwind.config.js`:

```javascript
theme: {
  extend: {
    colors: {
      primary: {
        // Ваша цветовая палитра
      },
    },
  },
}
```

### Стили

Глобальные стили и CSS-классы находятся в `src/styles/index.css`

## 🔧 Доступные команды

- `npm run dev` - Запуск dev сервера
- `npm run build` - Production сборка
- `npm run build:dev` - Development сборка
- `npm run lint` - Проверка кода линтером
- `npm run lint:fix` - Автоматическое исправление линтером

## 🐛 Отладка

### DevTools

- Redux DevTools - для отладки состояния
- React DevTools - для отладки компонентов

### Логирование

Включите verbose логирование в `src/utils/api.ts` для отладки API запросов.

## 📝 Лицензия

MIT

## 🤝 Поддержка

При возникновении вопросов или проблем, пожалуйста, создайте issue в репозитории проекта.


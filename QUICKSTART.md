# 🚀 Быстрый старт

## Что уже готово

✅ Полностью настроенный проект  
✅ Страницы авторизации и регистрации  
✅ Главная страница с дашбордами  
✅ Чат для AI интеграции  
✅ Современный UI/UX дизайн  
✅ Redux для управления состоянием  
✅ TypeScript + React 18  

## За 3 шага до запуска

### 1️⃣ Установить зависимости
```bash
cd /Users/andrejrusanov/projects/ai-dashboard-app
npm install
```

### 2️⃣ Запустить проект
```bash
npm run dev
```

### 3️⃣ Открыть в браузере
```
http://localhost:3000
```

## 🎯 Что вы увидите

### Страница входа (`/login`)
- Форма входа с валидацией
- Поля Email и Пароль
- Ссылка на регистрацию
- Красивый градиентный фон

### Страница регистрации (`/register`)
- Форма регистрации
- Валидация паролей
- Проверка совпадения паролей
- Условия использования

### Главная страница (`/`)
После входа вы увидите:

**Шапка:**
- Логотип и название приложения
- Информация о пользователе
- Кнопка уведомлений
- Кнопка выхода

**Дашборд:**
- 4 карточки статистики (пользователи, сессии, доход, конверсия)
- График аналитики за 7 месяцев
- Виджет последней активности

**Чат:**
- Боковая панель справа
- История сообщений
- Анимация печатания AI
- Готов к интеграции с backend

## 💡 Демо данные

Для входа используйте любые данные, например:
- **Email**: `demo@example.com`
- **Пароль**: `demo123`

Или зарегистрируйте новый аккаунт!

## 📁 Важные файлы

| Файл | Назначение |
|------|-----------|
| `src/app/App.tsx` | Главный компонент с роутингом |
| `src/pages/auth/LoginPage.tsx` | Страница входа |
| `src/pages/auth/RegisterPage.tsx` | Страница регистрации |
| `src/pages/main/MainPage.tsx` | Главная страница |
| `src/components/chat/ChatSection.tsx` | Компонент чата |
| `src/utils/api.ts` | API клиент (для AI интеграции) |
| `src/store/slices/authSlice.ts` | Redux slice для авторизации |
| `src/store/slices/chatSlice.ts` | Redux slice для чата |

## 🔌 Интеграция AI

### Вариант 1: OpenAI (рекомендуется)
```bash
npm install openai
```

См. подробные инструкции в файле `EXAMPLES.md`

### Вариант 2: Свой AI backend
Настройте `src/utils/api.ts`:

```typescript
export const chatApi = {
  sendMessage: async (message: string) => {
    const response = await apiClient.post('/your-ai-endpoint', { message });
    return response.data;
  },
};
```

## 🎨 Кастомизация

### Изменить цвета
Файл: `tailwind.config.js`
```javascript
colors: {
  primary: {
    600: '#your-color',  // Основной цвет
  },
}
```

### Изменить название
Файл: `public/index.html`
```html
<title>Ваше название</title>
```

### Добавить новый виджет
1. Создайте компонент в `src/components/dashboard/`
2. Импортируйте в `DashboardSection.tsx`
3. Добавьте в сетку

## 📚 Документация

- `README.md` - Общая информация о проекте
- `SETUP.md` - Детальная инструкция по установке
- `EXAMPLES.md` - Примеры расширения функционала
- `PROJECT_INFO.md` - Полная информация о структуре

## ⚡ Горячие клавиши (в dev режиме)

- Сохранение файла → автоматическая перезагрузка
- Redux DevTools → F12 → Redux tab
- React DevTools → F12 → Components/Profiler

## 🐛 Если что-то не работает

### Ошибка при установке
```bash
rm -rf node_modules package-lock.json
npm install
```

### Порт 3000 занят
Измените порт в `webpack.config.ts`:
```typescript
const PORT = env?.port || 3001;  // Новый порт
```

Или запустите с параметром:
```bash
npm run dev -- --env port=3001
```

### TypeScript ошибки
```bash
npm run lint:fix
```

## 🎉 Готово!

Ваш проект полностью готов к работе. Начните с:

1. ✅ Запустите `npm run dev`
2. ✅ Откройте http://localhost:3000
3. ✅ Зарегистрируйтесь или войдите
4. ✅ Изучите дашборд и чат
5. ✅ Начните интеграцию с AI

**Удачи в разработке! 🚀**

---

Есть вопросы? Все ответы в документации:
- Установка: `SETUP.md`
- Примеры: `EXAMPLES.md`
- Структура: `PROJECT_INFO.md`

